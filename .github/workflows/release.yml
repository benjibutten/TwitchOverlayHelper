name: Release

on:
  push:
    branches:
      - main
    paths:
      - src/**
      - TwitchOverlayHelper.slnx
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-main
  cancel-in-progress: true

env:
  PROJECT_PATH: src/TwitchOverlayHelper/TwitchOverlayHelper.csproj
  RUNTIME_ID: win-x64

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Compute release metadata
        id: meta
        shell: pwsh
        run: |
          # Date-based version: <year>.<month>.<sequence within the month>,
          # where the sequence is derived from existing release tags so no
          # external counter is needed.
          $now = [DateTime]::UtcNow
          $tagPrefix = "v$($now.Year).$($now.Month)."
          $maxSeq = 0

          foreach ($tag in (git tag --list "$tagPrefix*"))
          {
            $seq = 0
            if ([int]::TryParse($tag.Substring($tagPrefix.Length), [ref]$seq) -and $seq -gt $maxSeq)
            {
              $maxSeq = $seq
            }
          }

          $version = "$($now.Year).$($now.Month).$($maxSeq + 1)"
          $tagName = "v$version"
          $assemblyVersion = "$version.0"
          $releaseName = "Twitch Overlay Helper $version"
          $assetName = "TwitchOverlayHelper-win-x64.zip"

          "version=$version" >> $env:GITHUB_OUTPUT
          "tag_name=$tagName" >> $env:GITHUB_OUTPUT
          "assembly_version=$assemblyVersion" >> $env:GITHUB_OUTPUT
          "release_name=$releaseName" >> $env:GITHUB_OUTPUT
          "asset_name=$assetName" >> $env:GITHUB_OUTPUT

      - name: Restore dependencies
        shell: pwsh
        run: dotnet restore $env:PROJECT_PATH

      - name: Publish app
        shell: pwsh
        run: |
          $publishDir = Join-Path $PWD "artifacts/publish/$env:RUNTIME_ID"

          if (Test-Path $publishDir)
          {
            Remove-Item $publishDir -Recurse -Force
          }

          dotnet publish $env:PROJECT_PATH `
            -c Release `
            -r $env:RUNTIME_ID `
            --self-contained true `
            -p:ContinuousIntegrationBuild=true `
            -p:DebugType=None `
            -p:DebugSymbols=false `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -p:EnableCompressionInSingleFile=false `
            -p:PublishReadyToRun=true `
            -p:Version=${{ steps.meta.outputs.version }} `
            -p:AssemblyVersion=${{ steps.meta.outputs.assembly_version }} `
            -p:FileVersion=${{ steps.meta.outputs.assembly_version }} `
            -p:InformationalVersion="${{ steps.meta.outputs.version }}+${{ github.sha }}" `
            -o $publishDir

      - name: Create release zip
        shell: pwsh
        run: |
          $zipPath = Join-Path $PWD "artifacts/${{ steps.meta.outputs.asset_name }}"

          if (Test-Path $zipPath)
          {
            Remove-Item $zipPath -Force
          }

          Compress-Archive -Path "artifacts/publish/$env:RUNTIME_ID/*" -DestinationPath $zipPath

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v7
        with:
          name: twitch-overlay-helper-${{ steps.meta.outputs.version }}-win-x64
          path: artifacts/${{ steps.meta.outputs.asset_name }}

      - name: Write release notes
        shell: pwsh
        run: |
          @"
          Automated release from the main branch.

          Tag: ${{ steps.meta.outputs.tag_name }}
          Build: ${{ steps.meta.outputs.version }}
          Commit: ${{ github.sha }}
          Runtime: ${{ env.RUNTIME_ID }}

          Download the zip asset below, extract it to a folder, and run TwitchOverlayHelper.exe.
          Extracting over an existing installation preserves its location; app settings remain in LocalAppData.
          "@ | Set-Content -Path artifacts/release-notes.md

      - name: Create GitHub release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.meta.outputs.tag_name }}
          commit: ${{ github.sha }}
          name: ${{ steps.meta.outputs.release_name }}
          bodyFile: artifacts/release-notes.md
          artifacts: artifacts/${{ steps.meta.outputs.asset_name }}
          allowUpdates: true
          artifactErrorsFailBuild: true
          replacesArtifacts: true
          makeLatest: true
